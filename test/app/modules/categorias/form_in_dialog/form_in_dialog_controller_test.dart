import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:todo_app/app/modules/categorias/categorias_module.dart';

void main() {
  initModule(CategoriasModule());
  // FormInDialogController formindialog;
  //
  setUp(() {
    //     formindialog = CategoriasModule.to.get<FormInDialogController>();
  });

  group('FormInDialogController Test', () {
    //   test("First Test", () {
    //     expect(formindialog, isInstanceOf<FormInDialogController>());
    //   });

    //   test("Set Value", () {
    //     expect(formindialog.value, equals(0));
    //     formindialog.increment();
    //     expect(formindialog.value, equals(1));
    //   });
  });
}
